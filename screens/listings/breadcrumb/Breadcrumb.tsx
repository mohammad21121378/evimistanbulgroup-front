import { Breadcrumb as BaseBreadcrumb } from "@/components/breadcrumb/Breadcrumb";

export default function Breadcrumb() {
    return (
        <BaseBreadcrumb
            items={[{ label: 'Property for Sale in Turkey' }]}
        />
    )
}